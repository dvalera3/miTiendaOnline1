<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.1.xsd">

     	<!-- el dataSource definitivo con tus datos reales -->
	<bean id="xxx"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="com.mysql.cj.jdbc.Driver" />
		
		<!-- Forzamos el puerto libre 3308 de XAMPP -->
		<property name="url" value="jdbc:mysql://127.0.0.1:3308/bd_tienda_online?serverTimezone=UTC&amp;useSSL=false&amp;allowPublicKeyRetrieval=true"/>
		
		<!-- Forzamos tu usuario tienda -->
		<property name="username" value="tienda"/>		
		
		<!-- Forzamos tu contraseña tienda123 -->
		<property name="password" value="tienda123"/>
	</bean>
     
 
	

	<!-- la siguiente bean es la que vamos a usar todo el rato para utilizar hibernate -->
	<bean id="fff"
		class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
		<property name="dataSource" ref="xxx" />
		<property name="packagesToScan" value="modelo" />
		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">
					org.hibernate.dialect.MySQL5InnoDBDialect
				</prop>
				<prop key="hibernate.show_sql">true</prop>
				<prop key="hibernate.hbm2ddl.auto">update</prop>
			</props>
		</property>
	</bean>

	<!-- lo siguiente es para que la clase marcada como @Transactional realice todas 
	las operaciones de base de datos de cada metodo en forma de transaccion -->
	<tx:annotation-driven transaction-manager="transactionManager"/>
	<bean id="transactionManager"
		class="org.springframework.orm.hibernate4.HibernateTransactionManager">
		<property name="sessionFactory" ref="fff" />
	</bean>

</beans>
